export const metadata = {
  title: "XDefiant Italia - Aggiunta Post",
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
