export const metadata = {
  title: "XDefiant Italia - Approva Post",
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
