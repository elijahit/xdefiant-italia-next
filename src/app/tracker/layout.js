export const metadata = {
  title: "XDefiant Italia - Tracker",
};


export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
